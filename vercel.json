{
  "buildCommand": "cd app && npm install && npm run build && cp -r dist/* ../dist-temp && cd .. && rm -rf app && mv dist-temp app",
  "outputDirectory": "app",
  "installCommand": "cd app && npm install"
}
